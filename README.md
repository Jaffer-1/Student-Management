# Student-Management
This project implements a complete Student Management System, featuring role-based access control. It supports multiple user roles like Admin, Staff, and Guest, each with specific permissions to manage student records stored.
